This source tree contains the DirectUI Control Panel

You can build it with:
qmake
make

To setup themes and example applets, you also have to do a
make install

You may also have to install some plugins, which are in separate directory.

