#! /bin/sh

exec duicontrolpanel.launch -appletloader "$@" POSTFIX

# FIXME: launcher is disabled for the appletlauncher process
# until NB#220018 gets solved
# exec PREFIX duicontrolpanel.launch -appletloader "$@" POSTFIX

