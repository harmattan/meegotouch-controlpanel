#ifndef CATEGORY_FAKE_H
#define CATEGORY_FAKE_H

#include <QString>

void Category_setData (const QString& fileName,
                       const QString& name,
                       const QString& parentId,
                       const QString& title,
                       const QString& titleId,
                       const QString& subtitle,
                       const QString& subtitleId,
                       const QString& iconId);

#endif

